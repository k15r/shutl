[package]
name = "cli_tool"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4.4", features = ["derive", "string"] }
clap_complete = { version = "4.4", features = ["unstable-dynamic"] }
walkdir = "2.4"
regex = "1.10"
